kind: Cluster
apiVersion: kind.x-k8s.io/v1alpha4

nodes:
- role: control-plane
  # first approach (not common on production )
  # extraPortMappings:
  # - containerPort: 30000 # The port on the KinD node
  #   hostPort: 88         # The port on your actual machine
  #   listenAddress: "0.0.0.0"
- role: worker
  extraPortMappings:
  - containerPort: 30000
    hostPort: 8081
- role: worker
  extraPortMappings:
  - containerPort: 30000
    hostPort: 8082